using System.Collections.Generic;

namespace DiggersAndFlyers {
  public class SwimmingContainer {

    public List<ISwimming> container = new List<ISwimming> ();
    // container.Add (new Terrapin ());
    // container.Add (new BettaFish ());

    // return container;

  }
}